import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  btnContainer: {
    flex: 1,
    alignItems: 'center',
    padding: 8,
    margin: 10,
    elevation: 3,
  },
  btnIcon: {
    height: 17,
    width: 17
  }
});

export default styles;
